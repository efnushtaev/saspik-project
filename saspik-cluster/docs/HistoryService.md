[↑ Saspik-cluster](../README.md)

# ⧫ docs/HistoryService

#### Статус: запланировано

На данный момент не реализован: в коде сервера отсутствуют history-контроллер и history-сервис, `MqttController` инжектируется в `app.ts`, но не монтируется в роуты

Соответствующий эндпоинт (`GET /history/list`) в API отсутствует

### Планируемое поведение
- Сбор истории событий кластера (показания сенсоров, команды, срабатывания правил).
- HTTP-эндпоинт получения истории (например, `GET /history/list`).
- Хранение в MongoDB (отдельная коллекция) либо внешнем хранилище (Telegraf/InfluxDB).
