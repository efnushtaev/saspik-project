/**
 * Константы для конфигурации.
 */

/** Имя файла конфигурации по умолчанию. */
export const DEFAULT_CONFIG_FILE = 'rules.json';

/** Путь к файлу конфигурации по умолчанию (относительно рабочей директории). */
export const DEFAULT_CONFIG_PATH = './rules.json';

/** Интервал проверки изменений файла (мс). */
export const WATCH_INTERVAL_MS = 2000;